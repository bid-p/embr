def init(module):
    module.name = ":core"
    module.description = "Core Embr library"

def prepare(module, options):
    module.depends(
        ":modm-project.xml"
    )
    # add dependencies
    return True

def build(env):
    env.outbasepath = "embr/src"
    env.copy("./testsrc.hpp")
    # env.copy("./*.cpp")